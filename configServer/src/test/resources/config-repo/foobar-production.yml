info:
  component: foobar production
spring:
  cloud:
    config:
      server:
        git:
          basedir: target/config
  application:
    name: foobar
  jmx:
    default_domain: cloud.config.server
server:
  port: 8888
management:
  contextPath: /admin
foo:
    bar: production
    